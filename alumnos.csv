import os
import sys
import cv2
from pyzbar.pyzbar import decode

def leer_codigo(ruta_imagen):
    imagen = cv2.imread(ruta_imagen)

    if imagen is None:
        return None

    codigos = decode(imagen)

    if not codigos:
        return None

    return codigos[0].data.decode("utf-8")

def main():
    if len(sys.argv) != 2:
        print("Uso: python3 programa4.py carpeta")
        return

    carpeta = sys.argv[1]

    if not os.path.isdir(carpeta):
        print("La carpeta no existe.")
        return

    archivos = sorted(os.listdir(carpeta))

    for archivo in archivos:
        if archivo.lower().endswith(".png"):
            nombre = os.path.splitext(archivo)[0]
            ruta_completa = os.path.join(carpeta, archivo)
            id_leido = leer_codigo(ruta_completa)

            if id_leido is None:
                print(f"{nombre} -> No se pudo leer el codigo de barras")
            else:
                print(f"{nombre} -> ID: {id_leido}")

if __name__ == "__main__":
    main()